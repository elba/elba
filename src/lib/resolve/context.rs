//! Module `resolve/context` defines the `Context` type, which is passed around for all dependency
//! resolution actions.

pub struct Context {}

impl Context {
    pub fn new() -> Self {
        Context {}
    }
}
